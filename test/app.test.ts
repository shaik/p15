import {
    assertEquals,
    assertNotEquals,
  } from "https://deno.land/std/testing/asserts.ts";

import { app } from "../src/app.ts";

Deno.test("start server", async () => {

    assertEquals(1, 1);

})
